# 📖 دليل التعديل على موقع Triply

دليل شامل لتعديل محتوى وتصميم الموقع بسهولة

---

## 📁 هيكل المشروع

```
Triply_pjc/
├── src/
│   ├── data/           # 📊 كل البيانات والمحتوى
│   ├── components/     # 🧩 مكونات الموقع
│   ├── pages/          # 📄 الصفحات
│   ├── styles/         # 🎨 التصميم
│   └── assets/         # 🖼️ الصور والملفات
├── public/             # ملفات عامة
└── docs/               # النسخة المبنية للنشر
```

---

## 🎯 التعديلات السريعة

### 1️⃣ تعديل النصوص والمحتوى

كل النصوص موجودة في مجلد `src/data/`:

#### **الصفحة الرئيسية - Hero Section**
📍 الملف: لا يوجد ملف data منفصل - النصوص في `src/components/sections/HeroSection.jsx`
```jsx
// السطر 24-26: العنوان الرئيسي
<h1>رحلات متكاملة تبدأ من فكرة وتصل إلى تجربة لا تُنسى</h1>

// السطر 27-30: الوصف
<p>Triply توفر لك تخطيطاً ذكياً...</p>
```

#### **الخدمات**
📍 الملف: `src/data/services.js`
```javascript
{
  id: 1,
  title: 'اسم الخدمة',
  description: 'وصف الخدمة',
  icon: '✈️' // يمكن تغيير الأيقونة
}
```

#### **الوجهات السياحية**
📍 الملف: `src/data/destinations.js`
```javascript
{
  name: 'دبي - الإمارات',
  description: 'وصف الوجهة...',
  image: '/images/dubai.jpg',
  duration: '7 أيام'
}
```

#### **خيارات الحجز**
📍 الملف: `src/data/bookingOptions.js`
```javascript
// الوجهات المتاحة للحجز
export const bookingDestinations = [
  'دبي - الإمارات',
  'القاهرة - مصر',
  // أضف وجهات جديدة هنا
];

// الخدمات المتاحة
export const bookingServices = [
  { id: 'flight', label: 'حجز طيران', icon: '✈️' },
  // أضف خدمات جديدة
];

// مستويات الميزانية
export const budgetLevels = [
  { id: 'budget', label: 'اقتصادي', range: '2,000 - 5,000 ريال' },
  // عدل الأسعار حسب حاجتك
];
```

#### **روابط القائمة**
📍 الملف: `src/data/navigation.js`
```javascript
export const navigationLinks = [
  { label: 'الرئيسية', href: '#hero' },
  { label: 'الخدمات', href: '#services' },
  // أضف أو عدل الروابط
];
```

#### **Footer Links**
📍 الملف: `src/data/footerLinks.js`
```javascript
export const socialLinks = [
  { icon: 'instagram', href: 'رابط الانستقرام', label: 'Instagram' },
  // عدل الروابط حسب حساباتك
];
```

#### **نصوص المساعدة في النماذج**
📍 الملف: `src/data/formHelpers.js`
```javascript
export const formHelpers = {
  booking: {
    destination: {
      label: 'اختر وجهتك',
      placeholder: 'أين تريد السفر؟',
      helper: 'نص المساعدة...'
    }
  }
};
```

---

### 2️⃣ تعديل الألوان والتصميم

#### **الألوان الرئيسية**
📍 الملف: `tailwind.config.js`
```javascript
colors: {
  triply: '#0f5b4a',        // اللون الأساسي (أخضر)
  'triply-mint': '#71d4c1', // لون النعناع (فاتح)
  'triply-teal': '#2a9d8f', // لون التركواز
  'triply-sand': '#f4e4d7', // لون الرمل (خلفيات)
  'triply-accent': '#d59c56', // لون التمييز (ذهبي)
  
  // Dark Mode Colors
  'dark-bg': '#0a1628',
  'dark-surface': '#1a2332',
  'dark-elevated': '#2c3e50',
}
```

**كيف تغير الألوان:**
1. افتح `tailwind.config.js`
2. غير قيمة اللون (مثلاً `#0f5b4a` لأي لون تبيه)
3. احفظ الملف - رح يتحدث تلقائياً

#### **الخطوط**
📍 الملف: `tailwind.config.js`
```javascript
fontFamily: {
  sans: ['Cairo', 'sans-serif'],     // الخط الأساسي
  display: ['Tajawal', 'sans-serif'], // خط العناوين
}
```

---

### 3️⃣ تعديل الصور

#### **إضافة صور جديدة:**
1. ضع الصور في مجلد `src/assets/` أو `public/images/`
2. استخدمها في الكود:
```javascript
import myImage from '../assets/my-image.jpg';
// أو
<img src="/images/my-image.jpg" alt="وصف" />
```

#### **تغيير اللوقو:**
📍 الملف: `src/assets/logo-triply.svg`
- استبدل الملف بنفس الاسم
- أو غير المسار في الملفات اللي تستخدمه

---

### 4️⃣ إضافة أقسام جديدة

#### **مثال: إضافة قسم "عن الشركة":**

1. **أنشئ ملف البيانات:**
📍 `src/data/about.js`
```javascript
export const aboutData = {
  title: 'من نحن',
  description: 'وصف الشركة...',
  stats: [
    { label: 'سنوات الخبرة', value: '5+' },
  ]
};
```

2. **أنشئ المكون:**
📍 `src/components/sections/AboutSection.jsx`
```jsx
import { aboutData } from '../../data/about.js';

function AboutSection() {
  return (
    <section id="about" className="section-padding">
      <div className="max-w-6xl mx-auto px-6">
        <h2>{aboutData.title}</h2>
        <p>{aboutData.description}</p>
      </div>
    </section>
  );
}

export { AboutSection };
```

3. **أضفه للصفحة الرئيسية:**
📍 `src/pages/Home.jsx`
```jsx
import { AboutSection } from '../components/sections/AboutSection';

// داخل return:
<AboutSection />
```

---

## 🎨 تخصيص التصميم

### **تعديل المسافات:**
```jsx
// Padding (المسافات الداخلية)
className="p-4"    // صغير
className="p-8"    // متوسط
className="p-12"   // كبير

// Margin (المسافات الخارجية)
className="m-4"    // صغير
className="mb-8"   // من الأسفل فقط
```

### **تعديل الأحجام:**
```jsx
// النصوص
className="text-sm"   // صغير
className="text-base" // عادي
className="text-xl"   // كبير
className="text-3xl"  // أكبر

// العرض والارتفاع
className="w-full"    // عرض كامل
className="h-64"      // ارتفاع ثابت
```

### **الألوان في الكود:**
```jsx
// خلفيات
className="bg-triply"              // أخضر
className="bg-triply-mint"         // فاتح
className="dark:bg-dark-surface"   // داكن في Dark Mode

// نصوص
className="text-triply"            // نص أخضر
className="dark:text-dark-text-primary" // نص فاتح في Dark Mode
```

---

## 🚀 أوامر مهمة

### **تشغيل الموقع محلياً:**
```bash
cd Triply_pjc
npm run dev
```
ثم افتح: `http://localhost:5177`

### **بناء الموقع للنشر:**
```bash
npm run build
```

### **نسخ للنشر على GitHub Pages:**
```bash
Remove-Item docs -Recurse -Force -ErrorAction SilentlyContinue
Copy-Item -Path dist\* -Destination docs\ -Recurse -Force
```

### **رفع على GitHub:**
```bash
git add .
git commit -m "تحديث الموقع"
git push origin gh-pages
```

---

## 📱 التوافق مع الأجهزة (Responsive)

### **كيف تختبر:**
1. اضغط `F12` في المتصفح
2. اختر أيقونة الموبايل
3. جرب أحجام مختلفة

### **كيف تعدل للموبايل:**
```jsx
// مثال: حجم مختلف حسب الشاشة
className="text-sm sm:text-base md:text-lg lg:text-xl"
//        موبايل  تابلت صغير  تابلت      شاشة كبيرة
```

**معاني الاختصارات:**
- `sm:` - شاشات أكبر من 640px
- `md:` - شاشات أكبر من 768px
- `lg:` - شاشات أكبر من 1024px
- `xl:` - شاشات أكبر من 1280px

---

## 🌙 Dark Mode / Light Mode

### **كيف يشتغل:**
الموقع يتذكر اختيار المستخدم في localStorage ويطبقه تلقائياً.

### **إضافة دعم Dark Mode لعنصر جديد:**
```jsx
className="bg-white dark:bg-dark-surface text-triply-dark dark:text-dark-text-primary"
//        خلفية بيضاء في Light   نص أخضر في Light
//                 خلفية داكنة في Dark   نص فاتح في Dark
```

---

## ✨ Scroll Reveal Animations

### **كيف تضيف animation لعنصر جديد:**

```jsx
import { useScrollReveal } from '../../hooks/useScrollReveal';

function MyComponent() {
  const { ref, isVisible } = useScrollReveal({ threshold: 0.2 });
  
  return (
    <div ref={ref} className={isVisible ? 'reveal-fade-up' : 'reveal'}>
      المحتوى هنا
    </div>
  );
}
```

**أنواع الـ Animations:**
- `reveal-fade-up` - يطلع من تحت
- `reveal-fade-down` - ينزل من فوق
- `reveal-fade-left` - يجي من اليسار
- `reveal-fade-right` - يجي من اليمين
- `reveal-scale` - يكبر ويظهر
- `reveal-fade` - يظهر تدريجياً

---

## 🔧 حل المشاكل الشائعة

### **المشكلة: التغييرات ما تظهر**
**الحل:**
1. احفظ الملف (`Ctrl + S`)
2. انتظر 2-3 ثواني (Hot Reload)
3. إذا ما اشتغل، اعمل Refresh للصفحة (`Ctrl + R`)
4. إذا باقي المشكلة، أعد تشغيل السيرفر

### **المشكلة: السيرفر ما يشتغل**
**الحل:**
```bash
# تأكد إنك في المجلد الصحيح
cd c:\Users\Admin\OneDrive\Desktop\Triply--main\Triply_pjc

# شغل السيرفر
npm run dev
```

### **المشكلة: ألوان غريبة في Dark Mode**
**الحل:**
تأكد إنك ضفت `dark:` للألوان:
```jsx
// ❌ خطأ
className="bg-white text-black"

// ✅ صحيح
className="bg-white dark:bg-dark-surface text-black dark:text-white"
```

---

## 📞 جهات الاتصال (للتعديل)

### **تغيير معلومات الاتصال:**

📍 `src/components/sections/ContactSection.jsx`
```jsx
// السطر 55-58: البريد الإلكتروني
<a href="mailto:contact@triply.com">contact@triply.com</a>

// السطر 75-78: رقم الهاتف
<a href="tel:+966501234567">+966 50 123 4567</a>

// السطر 95-96: العنوان
<p>جدة - المملكة العربية السعودية</p>
```

📍 `src/components/ui/FloatingActionButton.jsx`
```jsx
// السطر 20: رقم الواتساب
const whatsappNumber = '+966501234567';
```

---

## 🎯 نصائح مهمة

1. **اختبر دائماً:**
   - جرب التعديلات في المتصفح قبل الرفع
   - اختبر Dark Mode و Light Mode
   - جرب على أحجام شاشات مختلفة

2. **احتفظ بنسخة احتياطية:**
   - قبل أي تعديل كبير، اعمل commit في Git
   ```bash
   git add .
   git commit -m "نسخة احتياطية قبل التعديل"
   ```

3. **تعديلات تدريجية:**
   - غير شي واحد بالمرة
   - اختبره
   - ثم انتقل للتعديل التالي

4. **استخدم الألوان من Tailwind:**
   - بدل ما تكتب لون مباشر
   - استخدم الألوان المعرفة في `tailwind.config.js`

---

## 📚 مصادر إضافية

- **Tailwind CSS Docs:** https://tailwindcss.com/docs
- **React Docs:** https://react.dev
- **Vite Docs:** https://vitejs.dev

---

## ✅ Checklist قبل النشر

- [ ] اختبرت كل الصفحات
- [ ] اختبرت Dark Mode و Light Mode
- [ ] اختبرت على موبايل وتابلت
- [ ] كل الروابط شغالة
- [ ] الصور تحمل بشكل صحيح
- [ ] معلومات الاتصال صحيحة
- [ ] النصوص خالية من الأخطاء الإملائية
- [ ] عملت `npm run build` بدون errors
- [ ] نسخت للمجلد `docs`
- [ ] رفعت على GitHub

---

**تم إعداد هذا الدليل لتسهيل التعديل على موقع Triply 🚀**

لأي استفسارات، راجع الكود أو اسأل المطور.
