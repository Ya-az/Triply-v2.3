# Triply – منصة تخطيط الرحلات (React + Tailwind)

إعادة تصميم كاملة تعتمد هوية Triply الجديدة مع معمارية مكونات قابلة لإعادة الاستخدام، وواجهة عربية أنيقة مبنية بـ **React** و **Tailwind CSS**.

## 🚀 نظرة عامة

- **إطار العمل**: Vite + React 18
- **التصميم**: Tailwind مع لوحة ألوان Triply المستخرجة من الشعار
- **المعمارية**: مكونات UI قابلة لإعادة الاستخدام + تخطيطات Layouts + بيانات منفصلة
- **التجربة**: تصميم مستوحى من السفر، زوايا مستديرة، تدرجات ناعمة، انتقالات سلسة

## 📁 هيكل المجلدات
```
Triply_pjc/
├── index.html
├── package.json
├── postcss.config.js
├── tailwind.config.js
├── vite.config.js
├── legacy/              # الملفات القديمة (أرشيف)
└── src/
	├── App.jsx
	├── main.jsx
	├── assets/
	│   └── logo-triply.svg
	├── components/
	│   ├── Footer.jsx
	│   ├── Navbar.jsx
	│   ├── sections/
	│   │   ├── BookingSection.jsx
	│   │   ├── ContactSection.jsx
	│   │   ├── DestinationsSection.jsx
	│   │   ├── HeroSection.jsx
	│   │   ├── ServicesSection.jsx
	│   │   └── TestimonialsSection.jsx
	│   └── ui/
	│       ├── Card.jsx
	│       ├── GlassButton.jsx
	│       ├── GoogleButton.jsx
	│       └── InputField.jsx
	├── data/
	│   ├── bookingOptions.js
	│   ├── destinations.js
	│   ├── footerLinks.js
	│   ├── navigation.js
	│   ├── services.js
	│   └── testimonials.js
	├── layouts/
	│   ├── AuthLayout.jsx
	│   └── MainLayout.jsx
	├── pages/
	│   ├── Home.jsx
	│   └── auth/
	│       ├── Login.jsx
	│       └── Signup.jsx
	└── styles/
		└── global.css
```

## 🧱 المكونات القابلة لإعادة الاستخدام

| المكون | الوصف |
|--------|-------|
| `GlassButton` | حالات متعددة (primary / secondary / accent / glass / outline) مع زوايا دائرية وظلال متدرجة |
| `Card` | لوحات زجاجية لعرض الخدمات والشهادات |
| `GoogleButton` | زر تسجيل دخول Google مع أيقونة |
| `InputField` | حقول إدخال مع دعم الأيقونات والتحقق |
| `Navbar` | شريط علوي ثابت مع قائمة متجاوبة وإدارة جلسة المستخدم |
| `Footer` | فوتر غني بالمحتوى وروابط اجتماعية وأزرار إصدار |
| `MainLayout` | يحوي الـ Navbar، المحتوى، والفوتر داخل بنية متماسكة |
| `AuthLayout` | تخطيط خاص بصفحات تسجيل الدخول والتسجيل |

## 🎨 التصميم والهوية

- **لوحة Triply** في `tailwind.config.js` مع مفاتيح `triply`, `triply-dark`, `triply-mint`, `triply-accent`, وغيرها.
- تدرجات مخصصة (`hero-gradient`, `card-gradient`, `soft-mesh`) تعكس ألوان الشعار.
- خطوط Google Fonts: Cairo + Poppins.
- تأثيرات Glassmorphism، ظلال ناعمة، وأنماط `@layer` مخصصة في `global.css`.

## ⚙️ أوامر التطوير

```bash
npm install
npm run dev       # تطوير محلي
npm run build     # بناء للإنتاج
npm run preview   # معاينة البناء
npm run deploy    # بناء ونشر إلى GitHub Pages
```

## ✅ أفضل الممارسات المطبقة

- فصل البيانات في مجلد `data/` لسهولة التحديث.
- أقسام الصفحة مقسمة إلى ملفات مستقلة داخل `components/sections/`.
- الإبقاء على كل المنطق (النماذج، الحالة) داخل المكونات مع الحفاظ على واجهة نظيفة.
- دعم الاستجابة الكاملة لجميع الأجهزة + تنسيق عربي RTL.

## � ملاحظات

- يحتاج المشروع لتثبيت الحزم (`npm install`) قبل التشغيل لأول مرة.
- التحذيرات المتعلقة بقواعد Tailwind (`@tailwind`, `@apply`) في `global.css` متوقعة من محلل CSS التقليدي.
- يمكن إضافة وضع ليلي (Dark Mode) لاحقًا إذ تم تفعيل `darkMode: 'class'` في Tailwind.

---

**© 2025 Triply – جميع الحقوق محفوظة**
